// The Party tab: shared party inventory lives here. Stubbed for now — just an
// empty-state placeholder until the inventory UI lands.
export default function PlayerPartyTab() {
  return (
    <div className="player-party-tab">
      <p className="player-party-empty">The party inventory is empty.</p>
    </div>
  );
}
